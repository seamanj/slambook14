cmake_minimum_required(VERSION 3.10)
project( visualizeGeometry   )

# set( CMAKE_BUILD_TYPE "Release" )
set(CMAKE_CXX_STANDARD 14)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

find_package(GLEW REQUIRED)
find_package(Eigen3 REQUIRED)
find_package(Threads REQUIRED)
find_package( Pangolin REQUIRED)
include_directories( ${Pangolin_INCLUDE_DIRS} ${GLEW_INCLUDE_DIRS})

# in osx and brew install
# include_directories( /usr/local/Cellar/eigen/3.3.3/include/eigen3 )

add_executable( visualizeGeometry  visualizeGeometry.cpp )
target_link_libraries(visualizeGeometry  Eigen3::Eigen ${Pangolin_LIBRARIES} ${GLEW_LIBRARIES})

add_executable(test_pangolin test_pangolin.cpp)
target_link_libraries(test_pangolin ${Pangolin_LIBRARIES} ${GLEW_LIBRARIES})